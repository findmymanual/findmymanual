Command to clean metadata

./exiftool.exe -ver

./exiftool.exe -all= -overwrite_original *.pdf

Command to check if pdf is clean

./exiftool.exe -r -if '$Author or $Creator or $Producer or $Title' -p '$FileName : $Author $Creator $Producer $Title' . > metadata_report.txt

